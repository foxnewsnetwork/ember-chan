import getChannelAttributes from 'dummy/utils/get-channel-attributes';
import { module, test } from 'qunit';

module('Unit | Utility | get channel attributes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getChannelAttributes();
  assert.ok(result);
});
